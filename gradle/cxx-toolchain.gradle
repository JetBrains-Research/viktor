model {
    toolChains {
        def cc = System.env.CC ?: ''
        gcc(Gcc) {
            if (cc.startsWith("gcc")) {
                path cc
            }
        }
        clang(Clang) {
            if (cc.startsWith("clang")) {
                path cc
            }
        }
    }
}
